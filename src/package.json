{
    "name": "s3-notification-lambda",
    "version": "1.0.0",
    "description": "Lambda function to handle S3 events and send notifications via SNS",
    "main": "index.js",
    "engines": {
      "node": ">=22.0.0"
    },
    "dependencies": {
      "@aws-sdk/client-sns": "^3.441.0"
    },
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "MIT"
  }